# TE_1.A_TFT — canonical results directory (PASS)

The only retained validation bundle under `results/` is:

  run_20251107_210503/

verified by:
  - summary.json: overall_pass == true (all criterion flags true)
  - PASS.txt present
  - TE_1.A_TFT/README.md, SESSIONS/1_3_TE_1A_INITIAL_RUNS.md

Prior FAILED or partial run_* directories were removed after inventory (2026-04-13)
to save disk; the pipeline remains reproducible via te1a_pipeline.py / run_te1a.py.

Recovery if a removed run ever needs to be re-examined:
  - Authoritative science for TE_1.A is the retained PASS run above; deleted runs all had
    summary.json overall_pass == false (or no summary), per inventory.
  - Older `results/run_*` trees removed from git for size may exist only in author-maintained
    backups outside this repository; they are not recoverable from the public archive alone.

# Python >= 3.10 required for ugp_discovery_lab (pyproject.toml). macOS /usr/bin/python3 is often 3.9.x — use a newer interpreter for venvs.
# The ugp_discovery_lab package also declares dependencies in ugp_discovery_lab/pyproject.toml.

numpy>=2.4.4
scipy>=1.12
pandas>=2.0
scikit-learn>=1.3
matplotlib>=3.8
seaborn>=0.13
PyYAML>=6.0.1
xgboost>=2.0
sympy>=1.12

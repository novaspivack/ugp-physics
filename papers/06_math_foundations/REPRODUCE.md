# REPRODUCE — Mathematical Foundations (JMP)

**Requirements:** Python 3.10+, `ugp_discovery_lab` at the clone root (`pip install -e ".[plots]"`).

```bash
cd ugp_discovery_lab
```

## 1) Gauge couplings derivation (Section 3 main result)

```bash
python3.10 -m ugp_discovery_lab.cli.ugp run-experiment \
  -c configs/experiments/gauge_couplings_unified.yaml --workers 2
```

**Expected:** g₁²=16/125 (0.00% err), g₂²=2329/5400 (1.48%), g₃²≈1.4857 (0.16%)

## 2) SU(2) harmonic mean rigidity (Appendix A.1)

```bash
python3.10 -m ugp_discovery_lab.cli.ugp run-experiment \
  -c configs/experiments/su2_rigidity_proof.yaml --workers 2
```

**Expected:** `SU(2) rigidity proof completed: True`

## 3) SU(3) Vandermonde rigidity (Appendix A.2)

```bash
python3.10 -m ugp_discovery_lab.cli.ugp run-experiment \
  -c configs/experiments/su3_rigidity_proof.yaml --workers 2
```

**Expected:** `SU(3) rigidity proof completed: True`

## 4) Figures 1 and 2 (ridge/GTE visualization)

```bash
cd ../papers/06_math_foundations/figures/ugp_gte_monograph
python3.10 visualize_ugp_gte_monograph.py
```

**Expected:** `ugp_gte_visualization_paper.png` and `ugp_gte_c1_field_paper.png` written.

## 5) Compile paper

```bash
cd ../papers/06_math_foundations
latexmk -pdf -interaction=nonstopmode algebraic_geometric_foundations_ugp.tex
```

## Notes

- `instantiation_factor.py`, `pentagon_symmetry.py`, `mobius_pslq_discovery.py`, `rg_flow_visualizer.py`, `prime_lock_search.py` are cited in Appendix B but not found in the repo — these computations are covered by `test_ugp_t_02_first_principles.py` and `ugp_uniqueness_sieve.py` (see `papers/05_uniqueness/REPRODUCE.md`).
- The gauge coupling figure (`gauge_couplings_comparison.png`) is generated by `gauge_couplings_unified` experiment run above.

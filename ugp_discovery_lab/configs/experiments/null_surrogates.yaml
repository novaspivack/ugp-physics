experiment:
  name: null_surrogates
  description: "Strict null models for alpha effects: permutation, circular shift, AAFT"
inputs:
  runs:
    - "results/reports/rg_sweep_summary.json"
  tests:
    n_perm: 500
    p_thresh: 0.01
  integrity:
    fail_on_warning: true
    require_real_data: true
  report:
    export_md: true
    export_json: true

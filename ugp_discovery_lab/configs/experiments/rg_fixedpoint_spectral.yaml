experiment:
  name: rg_fixedpoint_spectral
  description: "Independent spectral estimate of alpha* via linearized transfer map"
  inputs:
    runs:
      - "UGP_discovery_lab_runs/exp_*/results/reports/*.json"
  integrity:
    fail_on_warning: true
    require_real_data: true
  report:
    export_md: true
    export_json: true

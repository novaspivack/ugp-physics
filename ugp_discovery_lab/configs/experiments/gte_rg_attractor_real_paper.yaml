experiment:
  name: gte_rg_attractor_real
  description: "Apply RG operator to real GTE time series and recover α* from actual UGP evolution"
  inputs:
    runs:
      - "UGP_discovery_lab_runs/exp_20260413_deep_trajectories/results/reports/experiment_results.json"
  rg_iterations: 20
  report:
    export_md: true
    export_json: true

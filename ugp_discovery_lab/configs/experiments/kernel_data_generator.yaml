experiment:
  name: kernel_data_generator
  description: "Generate synthetic kernel series data for testing independent alpha estimators"
  generation:
    n_series: 20
    series_length: 200
  report:
    export_md: true
    export_json: true

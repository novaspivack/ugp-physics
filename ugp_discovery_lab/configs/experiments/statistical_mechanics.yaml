# DEPRECATED FOR PRE DYNAMICS PAPER — use statistical_mechanics_paper.yaml + run_entropy_analysis.py instead.
# This config uses glob patterns that match ZERO real files → silently falls back to synthetic data.
# The 159 violations it produced were from synthetic data, not real GTE trajectories.
# See UGP_discovery_lab/deprecated/README.md and PROVENANCE.md §Part B for details.
experiment:
  name: statistical_mechanics
  description: "DEPRECATED — see statistical_mechanics_paper.yaml. This config produces synthetic fallback data."
  
  inputs:
    runs:
      - "UGP_discovery_lab_runs/**/LE_*_summary.json"
      - "UGP_discovery_lab_runs/**/lawful_evolution_*_summary.json"
      - "UGP_discovery_lab_runs/**/gte_*_summary.json"
      
  coarse_graining:
    n_bins_b: 16
    n_bins_c: 16
    
  verification:
    monotonicity_tolerance: 1.0e-9
    
  report:
    export_md: true
    export_json: true
    plots: true

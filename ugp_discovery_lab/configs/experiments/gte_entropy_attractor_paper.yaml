experiment:
  name: gte_entropy_attractor
  description: "Entropy-attractor correlation + temporal shuffle null test — real GTE trajectories"
  inputs:
    runs:
      - "UGP_discovery_lab_runs/exp_20260413_deep_trajectories/results/reports/experiment_results.json"
  n_shuffles: 20
  report:
    export_md: true
    export_json: true

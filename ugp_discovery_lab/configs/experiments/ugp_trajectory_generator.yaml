experiment:
  name: ugp_trajectory_generator
  description: "Generate large datasets of real UGP trajectories for volume calculus analysis."
  
generation:
  n_trajectories: 100  # Generate 100 trajectories
  trajectory_length: 200  # Each trajectory has 200 steps
  
report:
  export_md: true
  export_json: true

experiment:
  name: "u1_coupling_derivation"
  description: "Derives U(1) gauge coupling from UGP Elegant Kernel constants"

integrity:
  fail_on_warning: true
  require_real_data: false  # This is a theoretical derivation, not data-dependent

report:
  export_md: true
  export_json: true

# This experiment is purely theoretical and doesn't require input data
# It tests the hypothesis that g₁² = (k_a² + k_b² + k_c²) / k_L2

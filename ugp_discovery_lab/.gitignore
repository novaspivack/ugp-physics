# Ephemeral lab runs: ignored from the repo root via `ugp_discovery_lab/.gitignore` (allowlist for frozen
# canonical `UGP_discovery_lab_runs/exp_*` trees). Do not re-add broad `exp_*` patterns here — they can block
# shipped runs such as `exp_20260412_rg_sweep_full`.
